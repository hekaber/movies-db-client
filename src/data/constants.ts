export enum APIResult {
    SUCCESS = "success",
    FAILURE = "failure"
}

export enum Enpoints {
    popular_movies = '/movie/upcoming'
}

export enum BodyMethods {
    POST = "POST",
    PUT = "PUT"
}
